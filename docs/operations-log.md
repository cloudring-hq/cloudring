# Operations Log

Use this file to record every material change to the local lab.

| Date | Action | Result | Notes |
| --- | --- | --- | --- |
| 2026-06-17 | Initial repository structure created | Done | Hyper-V elevation required |
| 2026-06-17 | Hyper-V fabric created | Done | `Platform-Fabric` internal switch, `PlatformNAT`, gateway `172.28.10.1` |
| 2026-06-17 | Ubuntu image path changed | Done | Azure VHD ignored NoCloud; switched to generic Ubuntu QCOW2 + qemu-img VHDX conversion |
| 2026-06-17 | VM topology created | Done | `platform-n1..n3`, 4 vCPU, 7 GiB RAM, 64 GiB OS disk, 180 GiB Longhorn disk, nested virt enabled |
| 2026-06-17 | k3s HA installed | Done | 3 server/etcd nodes, Kubernetes `v1.35.5+k3s1` |
| 2026-06-17 | Platform add-ons installed | Done | Cilium `1.19.5`, MetalLB `0.16.1`, Longhorn `1.12.0`, KubeVirt stable |
| 2026-06-17 | Storage test | Passed | Longhorn PVC bound and write/read probe returned `ok` |
| 2026-06-17 | KubeVirt test | Passed | `tenant-a/demo-cirros` VMI Ready on n2; virt-handler reports `/dev/kvm` present |
| 2026-06-17 | Tenant isolation test | Passed | tenant-a to tenant-b TCP attempt timed out with exit `143` under default-deny policy |
| 2026-06-17 | MetalLB test | Passed | Service `platform-tests/lb-test` got `172.28.10.100`; Windows host curl returned HTTP `200 OK` |
| 2026-06-17 | HA smoke test | Passed | Forced off `platform-n3`; API stayed available, MetalLB recovered after convergence, n3 restarted and returned Ready |
| 2026-06-17 | Provider target architecture docs | Done | Added cell-based provider architecture, tenancy model, scaling model, and roadmap |
| 2026-06-17 | Provider hardening layer | Done | Applied Pod Security labels, DNS egress policy, tenant object quotas, tenant RBAC, and APF templates |
| 2026-06-17 | Kyverno policy layer | Passed | Installed Kyverno chart `3.8.1`; `tenant-guardrails` Ready; tenant LoadBalancer dry-run rejected by admission |
| 2026-06-17 | Provider API contracts | Passed | Applied `Project`, `VirtualMachineClaim`, and `KubernetesClusterClaim` CRDs/RBAC; sample tenant-a objects created |
| 2026-06-17 | Tenant cluster path | Done | Added Cluster API/CAPK install script and tenant cluster template for next capacity milestone |
| 2026-06-17 | GitOps and portal scaffold | Done | Added GitOps overlays and Backstage catalog/template skeletons |
| 2026-06-17 | Provider layer verification | Passed | `verify-provider-layer.sh` confirms nodes Ready, CRs present, APF present, Kyverno denial works |
| 2026-06-17 | Provider controller deployed | Passed | `platform-system/provider-controller` reconciles Projects and VirtualMachineClaims |
| 2026-06-17 | Provider self-service VM test | Passed | `tenant-c/smoke-vm` claim created KubeVirt VM `claim-smoke-vm`; VMI Ready with IP `10.0.0.227` |
| 2026-06-17 | Provider/self-service verification hardened | Passed | Added status assertions, tenant-c LoadBalancer denial check, and listener-backed tenant isolation smoke |
| 2026-06-17 | Provider controller HA rollout | Passed | Scaled controller to 3 replicas with topology spread and PDB `minAvailable=2`; pods scheduled on n1/n2/n3 |
| 2026-06-17 | Provider controller pod-loss test | Passed | Deleted one controller pod; Deployment restored 3/3 and `verify-provider-layer.sh` stayed green |
| 2026-06-17 | Cluster API + CAPK installed | Passed | `clusterctl` initialized CAPI `v1.13.2`, kubeadm providers `v1.13.2`, and CAPK `v0.11.2`; all controller deployments Available |
| 2026-06-17 | KubernetesClusterClaim reconciliation | Passed | Provider controller created CAPI/CAPK objects for `tenant-a/demo-cluster`; CAPK control-plane VMI reached Running |
| 2026-06-17 | Tenant cluster bootstrap gate | Passed | Fixed CAPK tiny class to 2 vCPU, raised tenant-a quota, and reached kubeadm API availability |
| 2026-06-17 | Tenant cluster CNI | Passed | Installed Cilium `1.19.5` into `tenant-a/demo-cluster` through provider-side Helm Job |
| 2026-06-17 | Tenant cluster readiness verification | Passed | CAPI/KCP/Machine Ready; workload node `claim-demo-cluster-control-plane-rwp2f` Ready and kube-system pods Running |
| 2026-06-17 | Tenant CNI installer script | Passed | `iac/scripts/install-tenant-cni.sh` reran idempotently and upgraded Helm release `cilium` to revision 2 |
| 2026-06-17 | Final smoke suite | Passed | Provider layer, provider self-service VM, tenant isolation, and tenant cluster readiness verifiers passed |
| 2026-06-17 | Management control-plane HA hardening | Passed | CAPI core, kubeadm bootstrap/control-plane, CAPK, and cert-manager deployments scaled to 3 replicas with PDB `minAvailable=2` and pod spread across all 3 nodes |
| 2026-06-17 | Management HA verification | Passed | `verify-management-control-plane-ha.sh` confirmed all hardened management controllers `3/3`, PDBs present, and pods across 3 nodes; tenant cluster verifier stayed green |
| 2026-06-17 | Tenant worker scale attempt | Failed safely after rollback | Scaling `tenant-a/demo-cluster` worker replicas from `0` to `1` exhausted memory on `platform-n2`, degraded the local k3s node, and forced rollback to workers `0`; this validates the need for capacity admission before exposing worker scale in the lab |
| 2026-06-17 | Tenant control-plane restart finding | Degraded architecture gap | CAPK control-plane VM restarted on `platform-n1` with a new ephemeral containerDisk root; workload API recovered after CNI reinstall, but CAPI Machine stayed degraded with `NodeDeleted`, proving current tenant cluster template is not repair-safe or HA-ready |
| 2026-06-17 | Lab worker guardrail | Applied | Provider controller now caps tenant worker pools with `MAX_TENANT_CLUSTER_WORKERS_PER_POOL=0` in this Hyper-V profile to protect management HA until persistent tenant disks and capacity admission are implemented |
| 2026-06-17 | Tenant cluster clean rebuild | Passed | Recreated `tenant-a/demo-cluster`, installed Cilium via provider-side job pinned to the tenant VMI node, and restored CAPI/KCP/Machine Ready with worker replicas capped at `0` |
| 2026-06-17 | Final post-guardrail smoke suite | Passed | Management HA verifier, provider layer verifier, provider self-service VM test, tenant isolation test, and tenant cluster verifier passed after guardrail and rebuild |
| 2026-06-17 | Final state snapshot | Passed | All three management nodes Ready, no non-running pods, provider-controller `3/3` with PDB `minAvailable=2`, tenant control-plane VMI Running on `n2`, tenant Machine Ready=True, node memory under 75% |
| 2026-06-17 | Extended provider API contracts | Passed | Added and applied v1alpha1 CRD/RBAC/examples for `Image`, `Volume`, `Network`, `FirewallRule`, `BackupPlan`, and `AccessGrant`; backing reconciliation remains a future controller milestone |
| 2026-06-17 | Extended API verification | Passed | `verify-provider-api-contracts.sh` confirmed CRDs, demo objects, image catalog read via `platform:tenants`, tenant-a namespace writes, denial of cross-tenant volume write, and denial of tenant project creation |
| 2026-06-17 | Portal/GitOps contract scaffold | Done | Backstage catalog and tenant foundation template now expose image, network, firewall, volume, backup, and access grant workflows; GitOps docs state that tenant overlays use provider API objects |
| 2026-06-17 | Extended API backing reconciliation | Passed | Provider controller now statuses `Image`, creates PVC for `Volume`, creates NetworkPolicies for `Network` and allow-only `FirewallRule`, marks `BackupPlan` as `Accepted` with backend pending, and creates RoleBinding for `AccessGrant` |
| 2026-06-17 | Post-backing smoke suite | Passed | `verify-provider-api-contracts.sh`, `verify-provider-layer.sh`, and `verify-tenant-cluster-layer.sh` passed after provider-controller rollout restart; final snapshot had all nodes Ready, no non-running pods, PVC Bound, tenant CAPI Machine Ready, and node memory below 75% |
| 2026-06-17 | CapacityCell API and inventory reconciliation | Passed | Added cluster-scoped `CapacityCell` CRD/RBAC/example and provider-controller reconciliation from Kubernetes node inventory; `lab-hyperv` reports `Ready`, 3/3 nodes, `12000m` CPU, `20796Mi` memory, and `330` pods allocatable |
| 2026-06-17 | Post-CapacityCell smoke suite | Passed | `verify-provider-api-contracts.sh`, `verify-provider-layer.sh`, and `verify-tenant-cluster-layer.sh` passed; all management nodes Ready, no non-running pods, tenant control-plane VMI Running on `n2`, tenant workload node Ready, and node memory at 54/60/73% |
| 2026-06-17 | Capacity admission status | Passed | Added `spec.placement` and `status.admission` for `VirtualMachineClaim` and `KubernetesClusterClaim`; live `demo-vm` and `demo-cluster` are admitted to `lab-hyperv` with estimated footprints `1000m/256Mi` and `2000m/2048Mi` |
| 2026-06-17 | Tenant worker admission cap | Passed | Temporarily patched `tenant-a/demo-cluster` worker replicas to `1`; controller reported `WorkerReplicasCapped`, admitted workers stayed `0`, MachineDeployment replicas stayed `0`, no worker VMI was created, then spec was restored to `0` |
| 2026-06-17 | Post-admission smoke suite | Passed | `verify-provider-api-contracts.sh`, `verify-provider-layer.sh`, and updated `verify-tenant-cluster-layer.sh` passed after controller rollout; tenant cluster node and kube-system pods remained Ready/Running |
| 2026-06-17 | Reservation-aware CapacityCell status | Passed | `CapacityCell lab-hyperv` now reports `reserved` and `available` from admitted claims; live status showed 3 claims reserving `4000m` CPU and `2560Mi` memory, leaving `8000m` CPU and `18236Mi` memory available |
| 2026-06-17 | Capacity rejection path | Passed | Added and ran oversized VM claim test using a request larger than available capacity but not larger than total allocatable capacity; `tenant-a/capacity-too-large` was rejected with `InsufficientCapacity`, no backing KubeVirt VM was created, and the temporary claim was removed |
| 2026-06-17 | Post-reservation smoke suite | Passed | Updated provider API verifier, provider layer verifier, and tenant cluster verifier passed after provider-controller rollout; `lab-hyperv` remained Ready and tenant CAPK cluster stayed Ready |
| 2026-06-17 | Provider-controller leader election | Passed | Added Kubernetes `Lease` leader election in `platform-system`; one provider-controller pod holds `provider-controller` Lease while two standby replicas remain available behind the Deployment/PDB |
| 2026-06-17 | Provider-controller leader failover | Passed | Deleted the active Lease holder pod `provider-controller-7bf76566f7-p5hzr`; Deployment restored `3/3`, Lease moved to `provider-controller-7bf76566f7-j8jg5`, and `leaseTransitions` increased to `1` |
| 2026-06-17 | Post-leader-election smoke suite | Passed | Updated provider API verifier, provider layer verifier with Lease holder check, and tenant cluster verifier all passed; tenant CAPK workload node and kube-system pods remained Ready/Running |
| 2026-06-17 | CDI install and storage import path | Passed | Installed CDI `v1.65.0`; `verify-cdi-storage.sh` created blank DataVolume `platform-tests/cdi-blank-rootdisk`, DataVolume reached `Succeeded`, and the Longhorn PVC reached `Bound` |
| 2026-06-17 | CDI management HA hardening | Passed | `apply-management-control-plane-ha.sh` now sets `CDI.spec.infra.apiServerReplicas`, `deploymentReplicas`, and `uploadProxyReplicas` to `3`; CDI operator, apiserver, controller, and uploadproxy all reached `3/3` with PDB `minAvailable=2` |
| 2026-06-17 | Post-CDI HA smoke suite | Passed | `verify-management-control-plane-ha.sh`, `verify-cdi-storage.sh`, `verify-provider-api-contracts.sh`, `verify-provider-layer.sh`, and `verify-tenant-cluster-layer.sh` passed after CDI HA rollout |
| 2026-06-18 | CAPK persistent tenant root disks | Passed | Provider controller and tenant template now render CDI `DataVolume` root disks on Longhorn for new tenant Kubernetes nodes; CAPK templates expose KubeVirt masquerade ports `22` and `6443` |
| 2026-06-18 | CDI importer tenant egress | Passed | Provider controller now creates `allow-cdi-importer-egress` so CDI importer pods can pull registry images from default-deny tenant namespaces; registry DataVolume import reached `Succeeded` |
| 2026-06-18 | Tenant add-on job reachability fix | Passed | Found Cilium/KubeVirt masquerade same-node hairpin timeout: tenant API was reachable from `n1`/`n3` but not from the VMI host node `n2`; `install-tenant-cni.sh` and `verify-tenant-cluster-layer.sh` now prefer the CAPI service/controlPlaneEndpoint and schedule provider jobs on a Ready node different from the tenant VMI node when possible |
| 2026-06-18 | Persistent tenant cluster rebuild | Passed | Created `tenant-a/routable-cluster`; DataVolume `claim-routable-cluster-control-plane-gfgdl-rootdisk` reached `Succeeded`, Longhorn PVC stayed `Bound`, Cilium `1.19.5` installed from provider job on `n1`, and CAPI/KCP/Machine reached Ready |
| 2026-06-18 | Tenant control-plane VMI restart | Passed with cleanup | Deleted the routable cluster control-plane VMI; it recreated with pod IP `10.0.2.183` instead of `10.0.2.167`, preserved root PVC UID `b0e86f4c-f3c4-4904-b51e-7af4f8d90df7`, and CAPI/Machine returned Ready. Stale workload pods from the hard power-cut style restart were deleted and controllers recreated them Running |
| 2026-06-18 | Post-persistent smoke suite | Passed | `verify-management-control-plane-ha.sh`, `verify-cdi-storage.sh`, `verify-provider-api-contracts.sh`, `verify-provider-layer.sh`, and `verify-tenant-cluster-layer.sh` for `routable-cluster` passed; all management pods are Running/Succeeded and provider capacity reports `reserved=6000m`, `available=6000m` |
| 2026-06-18 | HA tenant service-class admission | Passed | Added `minControlPlaneReplicas`/`maxControlPlaneReplicas` to `CapacityCell.spec.serviceClasses`; `ha-tenant-kubernetes` advertises 3/5 control-plane service intent, while current `lab-hyperv` keeps `maxControlPlaneReplicas=1`. `verify-provider-api-contracts.sh` created a temporary 3-control-plane claim and confirmed `ControlPlaneReplicaLimitExceeded` without creating backing CAPI resources |
| 2026-06-18 | Multi-cell placement scoring | Passed | Provider controller now skips NotReady capacity cells for automatic placement and selects the Ready candidate with the best available CPU/memory score. `verify-provider-api-contracts.sh` created temporary `aaa-empty-cell` with no matching nodes plus an auto-placed VM claim, confirmed placement selected `lab-hyperv`, and cleaned the probe resources |
| 2026-06-18 | Explicit capacity reservations | Passed | Added provider-internal cluster-scoped `CapacityReservation` CRD and controller reconciliation for admitted VM and tenant Kubernetes claims. Live lab has 4 Active reservations totaling `6000m` CPU and `4608Mi` memory; `lab-hyperv` reports matching `reserved=6000m`, `available=6000m`. `verify-provider-api-contracts.sh`, `verify-provider-layer.sh`, `test-provider-self-service.sh`, and `verify-tenant-cluster-layer.sh` for `routable-cluster` passed, including checks that rejected probes do not create reservations and tenants cannot read `CapacityReservation` objects |
| 2026-06-18 | CapacityReservation TTL lifecycle | Passed | Added `reservationTTLSeconds`, `lastHeartbeatTime`, and `expiresAt` to `CapacityReservation.status`; provider-controller renews Active reservations with a 300-second TTL and ignores stale Active reservations during capacity accounting. `verify-provider-api-contracts.sh` now verifies heartbeat/TTL/future expiry, matching reservation totals, rejected-probe cleanup, and tenant RBAC denial. `verify-provider-layer.sh` passed after rollout; live reservations all show TTL `300` and future `expiresAt` |
| 2026-06-18 | Project quota usage accounting | Passed | Added `Project.status.quotaUsage` for admitted VM and tenant Kubernetes usage, and VM claim quota admission against `Project.spec.quotas`. A first tenant-cluster quota rejection probe exposed self-counting between two existing CAPI-backed clusters, so tenant-cluster quota admission was left for a later controller fix. Final `verify-provider-api-contracts.sh` and `verify-provider-layer.sh` passed; `demo-cluster` and `routable-cluster` were both Ready, four reservations were Active, and `lab-hyperv` reported `reserved=6000m`, `available=6000m` |
| 2026-06-18 | VM image catalog admission | Passed | Added public `cirros` catalog image and enforced VM image admission through Ready `Image` objects with project visibility checks and resolved KubeVirt `containerDisk` registry references. `verify-provider-api-contracts.sh` now rejects a missing-image VM claim with `ImageNotFound` before backing VM or capacity reservation creation. `verify-provider-layer.sh` and `verify-tenant-cluster-layer.sh` for `demo-cluster` and `routable-cluster` passed after rollout; `tenant-a/demo-vm` and `tenant-c/smoke-vm` are admitted with `cirros` resolved to `quay.io/kubevirt/cirros-container-disk-demo:latest`, four reservations remain Active, and `lab-hyperv` reports `reserved=6000m`, `available=6000m` |
| 2026-06-18 | Provider-controller metrics endpoint | Passed | Added built-in `/healthz` and Prometheus-style `/metrics` endpoints to every provider-controller replica, exposed them with Service `platform-system/provider-controller-metrics`, and added verifier checks for metrics scrape, active leader sample, and successful reconcile counter. Rollout passed; `verify-provider-layer.sh` reported one metrics leader and successful reconcile samples, and `verify-provider-api-contracts.sh` remained green afterward |
| 2026-06-18 | Provider routed tenant API gateway | Partial | Added provider-managed TCP proxy Deployments and MetalLB LoadBalancer Services in `capk-system` for tenant Kubernetes API endpoints, while keeping raw CAPK Services internal and tenant namespace LoadBalancer quota at `0`. `tenant-a/demo-cluster` is reachable through `172.28.10.103:6443` from Windows and passes `verify-tenant-cluster-layer.sh`; tenant LoadBalancer dry-run remains denied by Kyverno. `tenant-a/routable-cluster` still has gateway Service `172.28.10.104`, but its internal CAPK/KubeVirt masquerade Service times out on 6443 after VMI restart, so the provider controller now reports the claim as `Degraded` without `status.apiEndpoint` until backend recovery is completed |
| 2026-06-18 | Routed endpoint readiness status | Passed with degraded backend | Added provider-controller `/readyz` probing for tenant Kubernetes API endpoints before publishing `KubernetesClusterClaim.status.apiEndpoint`. After rollout, `tenant-a/demo-cluster` remains `Ready` with `172.28.10.103:6443` and Windows `curl.exe -k https://172.28.10.103:6443/readyz` returns `ok`; `tenant-a/routable-cluster` remains `Degraded` with no claim endpoint while Windows TCP to `172.28.10.104:6443` succeeds but HTTPS `/readyz` times out. `verify-provider-layer.sh`, `verify-provider-api-contracts.sh`, and `verify-tenant-cluster-layer.sh` for `demo-cluster` passed |
| 2026-06-18 | Tenant default-deny reconciliation fix | Passed | Fixed provider-controller `ensure_default_deny()` so tenant `default-deny` NetworkPolicies are actually applied during Project reconciliation, removed dead unreachable code after tenant-cluster admission return, and extended `verify-provider-layer.sh` to assert `default-deny` exists with both `Ingress` and `Egress` policy types. Final rollout passed; `verify-provider-layer.sh`, `verify-provider-api-contracts.sh`, and `verify-tenant-cluster-layer.sh` for `demo-cluster` remained green |
| 2026-06-18 | HA routed tenant API gateway recovery | Passed | Replaced the lab `alpine/socat` gateway proxy with a small `python:3.12-slim` TCP forwarder, added per-cluster proxy placement that avoids the tenant VMI host node, required hostname anti-affinity, safe hashed gateway names, and KCC endpoint conditions (`endpointReachable`, `EndpointReachable`, `lastKnownApiEndpoint`). `tenant-a/demo-cluster` and persistent-root `tenant-a/routable-cluster` now both publish routed endpoints (`172.28.10.103:6443`, `172.28.10.104:6443`), gateway Deployments are `2/2` on n1/n3, Windows `/readyz` returns `ok` for both, `probe-tenant-api-reachability.sh`, `verify-provider-layer.sh`, `verify-provider-api-contracts.sh`, and both tenant cluster verifiers passed |
| 2026-06-18 | Claim lifecycle finalizers | Passed | Added provider finalizer `platform.privatecloud.local/finalizer` to `VirtualMachineClaim` and `KubernetesClusterClaim`, with cleanup for provider-owned KubeVirt VM, CAPI/CAPK objects, tenant API gateway Service/Deployment, CAPI access NetworkPolicy, and `CapacityReservation` records. Added `verify-claim-lifecycle.sh`; it verified VM claim create/delete cleanup and rejected KCC finalizer release. A first run exposed missing RBAC `delete` on NetworkPolicies, which was fixed; final lifecycle verifier, `verify-provider-layer.sh`, and `verify-provider-api-contracts.sh` passed with live claims still Ready |
| 2026-06-18 | Dynamic tenant self-service RBAC | Passed | Found that `tenant-c` project reconciliation created the namespace and tenant admin RoleBinding but did not grant provider API self-service permissions unless a static RoleBinding existed. Provider-controller now creates `tenant-self-service` RoleBinding from each Project `adminsGroup` to ClusterRole `platform-tenant-self-service`. Added and ran `verify-self-service-tenant-workflow.sh`: tenant-c admin can create/delete a VM claim in tenant-c, cannot create in tenant-a, cannot read `CapacityReservation`, and delete cleans the VM plus reservation. `verify-provider-layer.sh` and `verify-provider-api-contracts.sh` passed afterward |
| 2026-06-18 | Tenant-cluster project quota admission | Passed | Enabled `KubernetesClusterClaim` admission against `Project.spec.quotas` using self-excluded `Project` usage, and fixed a worker-pool variable shadowing bug that made existing claims exclude `default` instead of the current claim name. `verify-provider-api-contracts.sh` now creates a third tenant cluster claim and confirms `ProjectQuotaExceeded` without backing CAPI resources or a reservation. After rollout, `demo-cluster` and `routable-cluster` recovered to `Ready`, four reservations are Active, `verify-provider-layer.sh`, `probe-tenant-api-reachability.sh`, `verify-self-service-tenant-workflow.sh`, and `verify-claim-lifecycle.sh` passed, and Windows `/readyz` returned `ok` for both routed tenant API endpoints |
| 2026-06-18 | Provider-controller immutable runtime image | Passed | Added `requirements.txt`, production `Dockerfile`, and a daemonless `build-provider-controller-image.sh` path that builds a local OCI image from cached `python:3.12-slim`, pre-installs pinned Python dependencies, and imports `localhost/platform-provider-controller:dev` into k3s containerd on all three nodes. Deployment now runs the image entrypoint with no pod-start `pip install` or ConfigMap code mount. `verify-provider-layer.sh` confirms the runtime image, leader Lease, metrics, and HA replicas; `verify-provider-api-contracts.sh` passed; routed endpoint probes passed from all controller pods, with the raw internal CAPK service remaining diagnostic-only for the known KubeVirt masquerade hairpin on the VMI host node |
| 2026-06-18 | HA read-only provider portal/API | Passed | Added `provider-portal` Python stdlib app, immutable local image build path, HA Deployment with 3 replicas, PDB `minAvailable=2`, read-only RBAC, and MetalLB Service `172.28.10.101`. The portal serves an admin dashboard plus compact `/api/summary` read model for projects, quotas, VM claims, tenant Kubernetes claims, capacity cells, active reservations, health, and recent events. `verify-provider-portal.sh` passed, Windows curl reached `/healthz`, `/api/summary` returned `ready=true` with 2 projects, 2 VMs, 2 tenant clusters, and 1 capacity cell, summary payload is about 62 KB, and portal pods are spread across n1/n2/n3 |
| 2026-06-18 | Admitted tenant-cluster delete lifecycle | Passed | Extended `verify-claim-lifecycle.sh` to temporarily raise `tenant-c` quota, create an admitted `KubernetesClusterClaim`, verify reservation, CAPI Cluster, KubeVirtCluster, KubeadmControlPlane, internal tenant API Service, provider gateway Deployment/Service, and access NetworkPolicy, then delete the claim and confirm all provider-owned resources are removed. Fixed provider-controller cleanup to delete the internal `claim-*-lb` Service as part of KCC finalization. Quota was restored to `tenant-c` (`cpu=2`, `memory=2Gi`, `tenantClusters=1`), live demo/routable clusters remained Ready, four expected reservations remained Active, `verify-provider-layer.sh`, `verify-provider-portal.sh`, and `verify-provider-api-contracts.sh` passed |
| 2026-06-18 | Provider portal claim write workflow | Passed | Added constrained portal write endpoints for `VirtualMachineClaim` and `KubernetesClusterClaim` create/delete, with server-side object construction, namespace/name/resource/CIDR/service-class validation, HTML escaping in the dashboard, `ENABLE_WRITE_API=true`, and portal RBAC limited to `create/delete` on the two claim resources. Rebuilt/imported `localhost/platform-provider-portal:dev` on n1/n2/n3 (`sha256:df1e129cdc12787ff53cab17185aee9825de86e3f20e9aad71833f9b2eec22ff`), rolled out 3/3 HA replicas, and extended `verify-provider-portal.sh` to create/delete temporary `tenant-c/portal-smoke-vm` and `tenant-c/portal-smoke-cluster` through the portal API. The write smoke passed, no portal leftovers or reservations remained, `verify-provider-layer.sh` and `verify-provider-api-contracts.sh` passed, and external `/api/summary` reported `ready=true`, `writeEnabled=true`, 2 VM claims, 2 tenant cluster claims, and 4 active reservations |
| 2026-06-18 | Flux GitOps reconciliation | Passed | Installed Flux v2.8.7 controllers in `flux-system`, added HA controller scaling/PDBs, and added an in-cluster immutable smart-HTTP Git source image for the local desired-state snapshot. Final `localhost/platform-gitops-source:dev` image was rebuilt/imported on n1/n2/n3 with digest `sha256:74c9e2b3cdd4da10456cbdf09aff6514a8e439c21390439d01a4420d29c3d49b`; the image serves `/platform.git` through `git-http-backend` with `safe.directory=*` for the non-root runtime. `platform-gitops-source` rolled out `3/3` with PDB `minAvailable=2`; `source-controller` runs one Ready leader plus two standby replicas with PDB `minAvailable=1`; `kustomize-controller`, `helm-controller`, and `notification-controller` are `3/3`. `GitRepository/platform-gitops` stored artifact revision `main@sha1:4e3b368f45ed4c95f1af7afdea5536bd96787a39`, `Kustomization/platform-base` applied the same revision, `verify-flux-gitops.sh`, `verify-provider-layer.sh`, and `verify-provider-portal.sh` all passed |
| 2026-06-18 | Cached provider portal read model | Passed | Added a per-replica `/api/summary` TTL cache to `provider-portal` with `SUMMARY_CACHE_TTL_SECONDS=5`, `SUMMARY_CACHE_STALE_SECONDS=60`, stale-if-error response metadata, cache invalidation after portal claim create/delete, and Prometheus-style `/metrics` for summary request/cache outcomes, refresh latency/errors, and write request counters. Rebuilt/imported `localhost/platform-provider-portal:dev` on n1/n2/n3 with digest `sha256:00c5208f20616997bf862eec0f0450951601be862373af5237844015fdccb067`; portal rolled out `3/3`, `verify-provider-portal.sh` passed with `cache=hit`, a manual fresh/read pair showed `miss` then `hit`, `/metrics` exposed cache and write counters, and `verify-provider-layer.sh` plus `verify-flux-gitops.sh` remained green |
| 2026-06-18 | VM-target BackupPlan backing reconciliation | Passed | Enabled KubeVirt `Snapshot` feature gate through `iac/kubernetes/platform/kubevirt-configuration.yaml`, added provider-controller RBAC for `snapshot.kubevirt.io`, and extended `BackupPlan` reconciliation to resolve Ready `VirtualMachineClaim` targets to backing KubeVirt VMs, create provider-managed `VirtualMachineSnapshot` recovery points, enforce simple retention, and report `Protected`/`lastSuccess`/`recoveryPoints`. Rebuilt/imported `localhost/platform-provider-controller:dev` on n1/n2/n3 with digest `sha256:780f46918d845c520ce538fa3d662f0506575003fa7b752ef1d9c677a31eaa50`; `tenant-a/demo-daily` now protects `claim-demo-vm` with snapshot `bkp-demo-daily-claim-demo-vm` (`Succeeded`, `readyToUse=true`). `verify-provider-api-contracts.sh` now asserts `Protected` plus ready KubeVirt snapshot and passed; `verify-provider-layer.sh` also passed |
| 2026-06-18 | VM RestoreRequest workflow | Passed | Added namespaced `RestoreRequest` CRD/RBAC and provider-controller reconciliation from `BackupPlan` recovery points to KubeVirt `VirtualMachineRestore` copy mode. The controller rejects target collisions, uses `volumeRestorePolicy=PrefixTargetName` and `volumeOwnershipPolicy=None`, labels restored VMs as `provider-restore`, and reports provider status from KubeVirt restore completion. Rebuilt/imported `localhost/platform-provider-controller:dev` on n1/n2/n3 with digest `sha256:6acfe0d6e640cc4781e0b37f98c67e6faf8994c3959747024ccc1c1690e6ce6b`; `verify-provider-api-contracts.sh` created temporary `tenant-a/contract-restore`, confirmed KubeVirt restore `restore-contract-restore` completed and VM `contract-restored-vm` had the restore label, then cleaned the objects |
| 2026-06-18 | GitOps snapshot after RestoreRequest | Passed | Rebuilt/imported `localhost/platform-gitops-source:dev` on n1/n2/n3 after adding `RestoreRequest` desired state and documentation. Flux reconciled `GitRepository/platform-gitops` and `Kustomization/platform-base` to a new `main@sha1:*` revision; this fixed a transient RBAC drift where tenant self-service lacked `restorerequests`. Final `verify-provider-api-contracts.sh`, `verify-provider-layer.sh`, and `verify-flux-gitops.sh` passed, and no temporary restore objects remained |
| 2026-06-18 | API fairness lanes for noisy tenants | Passed | Expanded Kubernetes API Priority and Fairness from one tenant lane to separate `platform-control`, `tenant-limited`, and `tenant-bulk-low` priority levels. Added FlowSchemas for provider/Flux/CAPI service accounts, platform admins, tenant interactive requests, and tenant list/watch bursts with tenant flows distinguished by namespace. Added `verify-api-fairness.sh`; it confirmed APF objects, tenant-c coverage, tenant RBAC denial for provider-internal/cross-tenant access, and a small impersonated tenant burst while `/readyz`, provider-controller, and Flux reads continued to succeed. `verify-provider-layer.sh` now also asserts the APF lanes and passed |
| 2026-06-18 | Provider-controller shard-aware reconciliation | Passed | Added stable namespace ownership, shard envs, per-shard Lease naming, namespace allowlist override, and `provider_controller_shard_info` metrics so tenant/cell controller loops can be split horizontally. Rebuilt/imported `localhost/platform-provider-controller:dev` on n1/n2/n3 with digest `sha256:65b83c0c634307b7eab55275354dec65ab3ff096aebf614eaaf1c324a24602bc`; rollout passed, `verify-controller-sharding.sh`, `verify-provider-layer.sh`, and `verify-provider-api-contracts.sh` passed |
| 2026-06-18 | Demo tenant cluster persistent-root rebuild | Passed | Recreated `tenant-a/demo-cluster` after the legacy containerDisk instance reset and correctly moved to `Degraded` with endpoint suppression. The finalizer removed old CAPI/CAPK, gateway, and reservation resources; the replacement cluster uses DataVolume `claim-demo-cluster-control-plane-ph9ld-rootdisk` on Longhorn, publishes `172.28.10.102:6443`, and reached CAPI/KCP/Machine Ready after Cilium `1.19.5` install. `verify-tenant-cluster-layer.sh` passed for both `demo-cluster` and `routable-cluster`; `verify-provider-layer.sh` and `verify-provider-api-contracts.sh` passed afterward |
| 2026-06-18 | Tenant API reachability probe endpoint discovery | Passed | Updated `probe-tenant-api-reachability.sh` to discover routed endpoints from `KubernetesClusterClaim.status.apiEndpoint` instead of hardcoding MetalLB addresses. The probe passed from all Running provider-controller pods for current routed endpoints `172.28.10.102:6443` and `172.28.10.104:6443`, plus diagnostic tenant ClusterIP services |
| 2026-06-18 | Provider-controller active-active shard topology | Passed | Added `render-provider-controller-shards.sh`, generated `iac/kubernetes/provider-controller/sharded-controller.yaml`, and added `verify-controller-shard-topology.sh` so production-style controller scale-out is a repeatable IaC artifact. The generated two-shard topology has three replicas per shard, shard-scoped PDBs and metrics Services, unique Leases `provider-controller-shard-0/1`, and exactly one global shard owner; server-side dry-run plus topology verification passed, while the live lab stayed on the verified single-shard `provider-controller` Deployment |
| 2026-06-18 | Provider-controller shard cutover rehearsal | Rolled back cleanly | Added `cutover-provider-controller-shards.sh` and made `verify-provider-layer.sh`, `verify-controller-sharding.sh`, and `verify-api-fairness.sh` topology-aware. A live rehearsal scaled the base Deployment to zero and rolled out `provider-controller-shard-0/1` with 3 replicas each, but tenant API smoke detected `demo-cluster` endpoint degradation caused by control-plane/gateway placement drift. Rollback restored the single-shard `provider-controller` Deployment, deleted shard Deployments/Services/PDBs/Leases, and later verifier runs passed |
| 2026-06-18 | Tenant API gateway placement and endpoint hysteresis | Passed | Added `TENANT_API_READYZ_FAILURE_THRESHOLD=3`, `endpointProbeReachable`, and `endpointFailureCount` status fields, plus gateway placement on managed pool `TENANT_API_PROXY_NODE_HOSTNAMES=n1,n3` with `externalTrafficPolicy=Cluster` while still avoiding the tenant VMI host. Rebuilt/imported `localhost/platform-provider-controller:dev` on n1/n2/n3 with digest `sha256:b60bae29ed14b26b8530a29a0f9197933f062269af2863e619188237f9f66a94`; `demo-cluster` endpoint `172.28.10.102:6443` and `routable-cluster` endpoint `172.28.10.104:6443` return `/readyz ok` from provider nodes, KCC status shows `endpointProbeReachable=true` and `endpointFailureCount=0`, and `probe-tenant-api-reachability.sh` passed for both clusters |
| 2026-06-18 | Post-shard/gateway verification suite | Passed | Re-applied the KCC CRD status schema with an explicit JSONPatch after `kubectl apply` updated only the last-applied annotation, then reran `verify-controller-sharding.sh`, `verify-provider-layer.sh`, `verify-api-fairness.sh`, `verify-controller-shard-topology.sh`, `verify-provider-api-contracts.sh`, and routed endpoint probes. Final live state: provider-controller `3/3` on n1/n2/n3, active Lease holder current, `demo` gateway `1/1` on n3, `routable` gateway `2/2` on n1/n3, both KCCs Ready, and all three management nodes Ready |
| 2026-06-18 | GitOps snapshot after shard/gateway hardening | Passed | Rebuilt/imported `localhost/platform-gitops-source:dev` after the shard cutover helpers, gateway placement, KCC endpoint hysteresis, CRD status fields, verifier updates, and documentation changes. Flux reconciled `GitRepository/platform-gitops` and `Kustomization/platform-base` to a new `main@sha1:*` revision; `verify-flux-gitops.sh` passed, KCC CRD `kubectl explain` sees `status.endpointProbeReachable`/`status.endpointFailureCount`, and both tenant API endpoints stayed Ready |
| 2026-06-18 | Provider-controller active-active cutover | Passed | Re-ran the shard cutover after gateway placement and endpoint hysteresis hardening. The base `platform-system/provider-controller` Deployment is scaled to `0`, `provider-controller-shard-0` and `provider-controller-shard-1` each run `3/3` replicas with independent Leases, shard `0/2` owns global reconciliation, shard `1/2` handles namespaced work only, and `verify-controller-sharding.sh`, `verify-provider-layer.sh`, `verify-api-fairness.sh`, `verify-controller-shard-topology.sh`, `verify-provider-api-contracts.sh`, routed tenant API probes, and a 10-cycle KCC stability loop passed |
| 2026-06-18 | GitOps desired state for sharded controller | Passed | Added the provider-controller manifests to `gitops/platform/kustomization.yaml`: `rendered-controller.yaml` supplies namespace/RBAC/rollback material, `sharded-controller.yaml` supplies the active runtime, and a kustomize patch keeps the base `provider-controller` Deployment at `replicas: 0`. Flux reconciled `platform-base` to the sharded desired state, base `provider-controller` is `0/0`, both shard Deployments are `3/3`, and the post-reconcile gate passed: `verify-controller-sharding.sh`, `verify-provider-layer.sh`, `verify-api-fairness.sh`, `verify-provider-api-contracts.sh`, `verify-management-control-plane-ha.sh`, routed tenant API probes for `demo-cluster` and `routable-cluster`, and both tenant cluster layer verifiers |
| 2026-06-18 | Bounded API fairness smoke | Passed | `verify-api-fairness.sh` originally used an unbounded 80-iteration tenant burst and exposed transient API `/readyz` `etcd failed` under the tiny Hyper-V profile. The verifier now uses per-request `--request-timeout`, defaults to 5 tenant iterations and 10 platform probes, and still validates APF lanes, tenant RBAC isolation, and platform responsiveness during noisy tenant reads |
| 2026-06-18 | Lab management and MetalLB probe hardening | Passed | Replaced old CrashLooping management/MetalLB pods after n1 resource pressure exposed 1-second probe fragility. `apply-management-control-plane-ha.sh` now gives CAPI/CAPK manager pods requests/limits, 5-second health probe timeouts, and soft placement away from n1; the Hyper-V installer applies lab patches for `metallb-frr-k8s-statuscleaner` and `metallb-frr-k8s`. Final cooldown had no CrashLoop/Pending pods, MetalLB FRR `3/3`, statuscleaner `1/1`, and management controllers/CDI/cert-manager `3/3` with PDB `minAvailable=2` |
| 2026-06-18 | CAPI routed tenant endpoint inspection | Passed | CAPI/KCP inspection for `demo-cluster` degraded after active management controllers used raw CAPK ClusterIP endpoints and hit the known KubeVirt masquerade hairpin path. Provider-controller now syncs CAPI `Cluster.spec.controlPlaneEndpoint` and generated kubeconfig Secrets to the provider routed VIPs. The current lab sets `TENANT_API_KUBECONFIG_INSECURE_SKIP_TLS_VERIFY=true` because existing tenant apiserver certs do not include the post-allocated VIP SANs; production should pre-allocate DNS/VIPs and include them in kubeadm cert SANs instead. Both `demo-cluster` and `routable-cluster` returned to CAPI/KCP/Machine `Available=True`, tenant verifiers passed, and routed `/readyz` works from Windows |
| 2026-06-18 | Tenant API gateway disruption budgets | Passed | Provider-controller now reconciles a `policy/v1` PDB for each provider-managed tenant API gateway in `capk-system` with `minAvailable=1` and deletes it during `KubernetesClusterClaim` finalization. Live gateways now have PDBs: `tenant-a-claim-demo-cluster-api` protects the single proxy with `allowedDisruptions=0`, and `tenant-a-claim-routable-cluster-api` protects the two-proxy path with `allowedDisruptions=1`. `verify-provider-layer.sh` asserts gateway PDB coverage, and `verify-claim-lifecycle.sh` checks admitted KCC PDB create/delete cleanup |
| 2026-06-18 | Tenant control-plane restart drill | Passed | Added `verify-tenant-control-plane-restart.sh`, a confirmation-gated destructive recovery drill for persistent-root CAPK tenant clusters. Ran it against `tenant-a/routable-cluster`: deleted VMI `claim-routable-cluster-control-plane-gfgdl`, KubeVirt recreated it with UID `347e41e8-45f4-4988-8a5f-cd6e36a417fd` and IP `10.0.2.42`, Longhorn root PVC UID stayed `b0e86f4c-f3c4-4904-b51e-7af4f8d90df7`, CAPI/KCP/Machine and KCC returned Ready, routed `/readyz` on `172.28.10.104:6443` succeeded, stale tenant `kube-system` pods were force-cleaned, the namespace settled back to Running pods, and tenant layer plus routed reachability verifiers passed |
| 2026-06-18 | Capacity admission lock | Passed | Added per-`CapacityCell` `coordination.k8s.io/Lease` admission locking for VM and tenant Kubernetes claims, `status.admission.observedGeneration` fast-pathing for already-admitted generations, CRD schema support, and RBAC `delete` on Leases so controllers can release locks. `verify-capacity-admission-lock.sh` holds synthetic Lease `platform-system/capacity-admission-lab-hyperv`, confirms a temporary `tenant-c/admission-lock-vm` stays `PendingAdmission` with no backing VM/reservation, releases the Lease, then verifies the claim reaches `Ready` with Active reservation and cleans up while restoring temporary tenant-c quota. Rebuilt/imported `localhost/platform-provider-controller:dev` on n1/n2/n3 with digest `sha256:61214f0a7e2d4363382a1d322024d9c1b80c0c0f0326c4e2f6f1a59c898d0288`; `verify-provider-layer.sh`, `verify-controller-sharding.sh`, `verify-tenant-cluster-layer.sh` for `routable-cluster`, `verify-provider-api-contracts.sh`, and routed reachability probe passed afterward |
| 2026-06-18 | Lab footprint right-sizing | Passed | The duplicate `tenant-a/demo-cluster` VMI on n1 drove n1 load high enough to fail local k3s `/readyz` on etcd in the tiny 3x4 vCPU/7 GiB Hyper-V profile. Deleted the nonessential `demo-cluster` claim and provider finalizer cleaned the KCC, CAPI/CAPK objects, gateway, reservation, and VMI. n1 returned to `/readyz ok`; the active live footprint is now `tenant-a/routable-cluster`, `tenant-a/demo-vm`, and `tenant-c/smoke-vm`, with three Active reservations totaling `4000m` CPU and `2304Mi` memory and `lab-hyperv` reporting `available=8000m` |
| 2026-06-19 | Provider portal tenant write authorization | Passed | Added static-token bearer authentication for provider-portal write requests as the lab implementation of the future OIDC caller contract. `platform-system/provider-portal-auth` maps demo tokens to subjects, groups, and allowed namespaces; portal writes require either `platform:admins` or the target `Project.spec.adminsGroup`, so a tenant token cannot create/delete claims in another namespace even though the portal service account owns the substrate write permission. The UI stores an optional write token locally and sends it as `Authorization: Bearer ...`; read-only summary/health/metrics remain unauthenticated. The portal cache now keeps the last summary payload when writes mark the cache stale, so transient API 429/backpressure returns stale data instead of failing the console. Added the portal manifest to `gitops/platform/kustomization.yaml` so Flux owns the HA portal and auth Secret. Rebuilt/imported `localhost/platform-provider-portal:dev` on n1/n2/n3 with digest `sha256:19d49bff177df132d606c728bb9de6d145cde5f792648270837c08eac6591522`; rollout reached `3/3`, and `verify-provider-portal.sh` confirmed unauthenticated/invalid-token `401`, wrong-tenant `403`, namespace/project mismatch `400`, valid tenant-c create/delete for temporary VM and tenant Kubernetes claims, metrics counters, and cleanup |
| 2026-06-19 | Provider portal autoscaling guardrail | Passed | Added a Flux-owned `autoscaling/v2` HPA for the stateless `provider-portal` Deployment with `minReplicas=3`, `maxReplicas=9`, CPU target `60%`, memory target `75%`, bounded scale-up policies, and a 300-second scale-down stabilization window. The Deployment no longer relies on GitOps fixed `spec.replicas`; the HPA owns the scale subresource while the PDB keeps `minAvailable=2`. Updated `verify-provider-portal.sh` to assert HPA target/bounds/metrics and wait for `ScalingActive=True`/`AbleToScale=True`, and updated `verify-flux-gitops.sh` to assert Flux-applied HPA bounds. Rebuilt/imported `localhost/platform-gitops-source:dev` on n1/n2/n3 with digest `sha256:d22fcf95b954a13a54185f583705aecb54967e2f9876b7aad4dfb11e2d40817e`; Flux applied `main@sha1:c411a42a3d6e4dcdf5f0629730b5723c22f15612`. The HPA scaled the portal from 3 to 6 replicas during verifier load and all six pods stayed Running across n1/n2/n3; `verify-provider-portal.sh`, `verify-flux-gitops.sh`, and `verify-provider-layer.sh` passed, and external `/api/summary` remained `ready=True` |
| 2026-06-19 | Provider portal tenant write rate limiting | Passed | Added an in-process per-replica write limiter to `provider-portal` keyed by authenticated subject and tenant namespace before the portal service account calls Kubernetes, closing the APF visibility gap where the apiserver sees all portal writes as the same service account. Lab defaults are `8` write attempts per `10s` per subject/namespace/replica; production should back this with a shared/global limiter. The summary health block now exposes write rate-limit config, `429` responses include `retryAfterSeconds` and `Retry-After`, and write metrics record status `429`. Rebuilt/imported `localhost/platform-provider-portal:dev` on n1/n2/n3 with digest `sha256:68d9f45c09db5b67682435dcc454d54e12a8b9868dd1a0a6447cde7ec878f383`; rollout reached `3/3`. `verify-provider-portal.sh` now tests a selected Ready portal pod through its PodIP instead of `kubectl exec` inside the constrained portal container, and confirmed unauthenticated/invalid-token `401`, wrong-tenant `403`, namespace/project mismatch `400`, valid tenant-c create/delete cleanup, noisy tenant write burst `429`, and rate-limit metric coverage |
| 2026-06-19 | Project quota admission lock | Passed | Added a first-pass per-`Project` `coordination.k8s.io/Lease` admission lock (`project-quota-admission-<project>`) around project quota admission, capacity admission, and reservation/status writes for VM and tenant Kubernetes claims. Invalid project references are rejected before taking the lock so stale project locks cannot mask `ProjectNotFound` or namespace mismatch errors; the normal admission path still revalidates the project under the lock. Rebuilt/imported `localhost/platform-provider-controller:dev` on n1/n2/n3 with digest `sha256:b168a9e88ab69ae05f97c8c4e6e07ea7949e6fd761dcf8dedc9547f96cfcfcbe`; all six sharded controller pods were recycled and run imageID `sha256:f73a839344882b077bd6e5fbf261651ddac9d7d8e1061346fc1aa8a22321bc91`. `verify-project-quota-admission-lock.sh` held synthetic Lease `platform-system/project-quota-admission-tenant-c`, confirmed a temporary VM claim stayed `PendingAdmission` with `ProjectQuotaAdmissionLocked` and no VM/reservation, released the Lease, verified the claim reached `Ready` with Active reservation, and cleaned up while restoring tenant-c quotas. `verify-capacity-admission-lock.sh`, `verify-provider-api-contracts.sh`, `verify-controller-sharding.sh`, and `verify-provider-layer.sh` passed afterward |
| 2026-06-19 | Shared provider portal write rate limiting | Passed | Replaced the portal's per-replica in-memory write limiter with a shared Kubernetes `Lease`-backed fixed-window limiter keyed by authenticated subject and tenant namespace. The limiter stores counters in `platform-system` Leases with optimistic `resourceVersion` updates, exposes `scope=shared` and `backend=kubernetes-lease` in `/api/summary`, and fails closed with `503` if the shared backend cannot be updated. Added least-privilege namespaced `Role`/`RoleBinding` `provider-portal-rate-limit` for Lease `get/create/update/patch`. Rebuilt/imported `localhost/platform-provider-portal:dev` on n1/n2/n3 with digest `sha256:7037ef71230dd09f2eb08f52635f744c044b37053a178a5c2d9ab4cfac5bb68a`; `verify-provider-portal.sh` now enumerates Ready portal pods, distributes the noisy tenant burst across three pod IPs, and confirms a shared `429` before Kubernetes claim creation. `verify-provider-layer.sh` and `verify-provider-api-contracts.sh` passed afterward |
| 2026-06-19 | Durable self-service audit events | Passed | Added namespaced `SelfServiceAuditEvent` CRD as the provider API audit surface for portal self-service writes. The portal now records allowed, rejected, rate-limited, and backend-error create/delete attempts with subject, subject hash, groups, project, resource, outcome, HTTP status, request path, and timestamp; `/api/summary` includes recent audit records for the admin UI. Tenant self-service RBAC can read audit records in its own namespace but cannot create/delete them or read another tenant's audit records, while platform admins retain provider-wide access. Rebuilt/imported `localhost/platform-provider-portal:dev` on n1/n2/n3 with digest `sha256:b8470c7bb2c6867b3ce30d645e7da04b974f7aacb16d5df206e9fb2718995a2c`; `verify-provider-portal.sh` confirmed audit records for allowed create/delete and shared rate-limited attempts, and `verify-provider-api-contracts.sh` now verifies the CRD plus tenant audit RBAC. `verify-provider-layer.sh`, `verify-provider-api-contracts.sh`, `verify-provider-portal.sh`, `verify-flux-gitops.sh`, and `verify-api-fairness.sh` passed after Flux applied `main@sha1:3d4ecbd2e320d8d21e872432f373827bb5ef88d7` |
| 2026-06-19 | Provider portal semantic ETag read caching | Passed | Added weak semantic `ETag` support to `/api/summary`, normalizing out volatile cache metadata, per-replica generation timestamps, transient Kubernetes Events, and reservation heartbeat/expiry timestamps while keeping durable inventory and `SelfServiceAuditEvent` changes visible. `If-None-Match` now returns `304 Not Modified` for unchanged semantic provider inventory, reducing dashboard/API polling payloads before a production shared inventory service exists. Rebuilt/imported `localhost/platform-provider-portal:dev` on n1/n2/n3 with digest `sha256:ca12114284fbb7e431c99465a579c54c187bde9cbe8dd718a7af9ff72ee14e72`; `verify-provider-portal.sh` now asserts `ETag`/`304` and passed, a Python client through the MetalLB endpoint `http://172.28.10.101/api/summary` received `304` with an empty body, and `verify-provider-layer.sh` plus `verify-provider-api-contracts.sh` passed afterward. The GitOps source snapshot was rebuilt and `verify-flux-gitops.sh` passed after Flux applied the updated desired state |
| 2026-06-19 | Provider API read-load gate | Passed | Added `provider_portal_summary_response_bytes_total` metrics and `iac/scripts/verify-provider-api-load.sh` as a bounded read-load verifier for the admin/API path. The verifier waits for steady-state health, sends concurrent conditional `/api/summary` reads across all Ready portal replicas, requires high `304` ratio, response-byte savings, and p95 latency within the lab budget, and continuously probes Kubernetes `/readyz`, provider-controller, Flux, and the routed tenant API during the load. Rebuilt/imported `localhost/platform-provider-portal:dev` on n1/n2/n3 with digest `sha256:69f0b9239a42698babe328ddc16ad41e3e4933ddbf7e84c6991e58f292ed1518`; `verify-provider-portal.sh` passed with the byte metric assertion, and the final `verify-provider-api-load.sh` run passed with 270 conditional reads across 9 portal replicas, `304_ratio=100.0%`, `bytes_saved=100.0%`, and p95 `1093.4ms` |
| 2026-06-19 | Provider portal extended self-service write surface | Passed | Expanded `provider-portal` create/delete endpoints and UI from VM/KCC-only to the safe bounded provider API subset: `VirtualMachineClaim`, `KubernetesClusterClaim`, `Volume`, `Network`, `FirewallRule`, and `AccessGrant`. The portal now validates CRD-compatible enum sets, resource quantities, CIDRs, IPs, ports, identity subjects, roles, and target kinds before creating provider API objects; GitOps seeds the Deployment at `replicas=3` while HPA still owns horizontal scale from 3 to 9. Rebuilt/imported `localhost/platform-provider-portal:dev` on n1/n2/n3 with digest `sha256:312c93c78c63480423cb3513c17eee903757ee15327aaf29661946b6823ab1a0`. Flux reconciled the updated RBAC/CRDs/portal desired state to `main@sha1:5ab2213d4dcfbd7ce5b232b96ac2b41d23b4482c`; `verify-provider-portal.sh` created/deleted temporary VM, KCC, volume, network, firewall, and access-grant resources through the portal, verified shared rate limiting across three portal pods, and found durable audit records plus metrics for all new write resources. Follow-up gates passed: `verify-flux-gitops.sh`, `verify-provider-api-load.sh` with 150 conditional reads across 5 Ready portal replicas (`304_ratio=100.0%`, `bytes_saved=100.0%`, p95 `1682.5ms`), `verify-provider-layer.sh`, `verify-api-fairness.sh`, and `verify-provider-api-contracts.sh` |
| 2026-06-19 | Durable admission decision journal | Passed | Added cluster-scoped provider-internal `AdmissionJournal` CRD/RBAC plus provider-controller write-once journal creation for VM and tenant Kubernetes admission decisions. Journals capture claim UID/generation, project quota snapshot, selected capacity cell/service class, lock names, controller shard identity, decision, reason, and observed timestamp; tenant self-service RBAC cannot read them, while the portal `/api/summary` exposes recent records for admins. Rebuilt/imported `localhost/platform-provider-controller:dev` on n1/n2/n3 with manifest digest `sha256:7410b3cc2b3ff63b2969ec8a46f058f2f3f0bebf3ac1c5c03662b4d92c075eb8` and runtime imageID `sha256:61329073634deff20b6c4225831b5b61a66aa4384f4d12d0751ba617b2679083`; rebuilt/imported `localhost/platform-provider-portal:dev` with manifest digest `sha256:d1ea5cbbf5f96c0879c661fc4eac12e35b74667c41d1b4e8b1e4fb08431e623b` and runtime imageID `sha256:87a8b1edb716507c9cf2c0289ae7932d40bce0d7205c27c93435ab28c3e2b734`. Recycled stale local `:dev` cache pods until all six controller shard pods and all portal pods ran the new content. `verify-provider-api-contracts.sh` found AdmissionJournal records for live admitted claims and quota/capacity/image/placement/HA probes by UID; `verify-provider-portal.sh`, `verify-provider-layer.sh`, `verify-flux-gitops.sh`, `verify-controller-sharding.sh`, and `verify-api-fairness.sh` passed. The read-load gate passed with 90 conditional summary reads across 3 Ready portal pods, `304_ratio=100.0%`, `bytes_saved=100.0%`, and p95 `2040.4ms`; external summary returned `ready=True`, `nonRunningPods=0`, 8 admission journals, and conditional `304`. |
| 2026-06-19 | Control-plane durable record retention | Passed | Added global-shard retention for provider-internal `AdmissionJournal` and tenant `SelfServiceAuditEvent` records, with lab defaults of 7 days, 5000 admission journals per `projectRef`, and 1000 audit events per namespace. The controller now emits `provider_controller_retention_deleted_total` metrics and deletes old/excess records without touching fresh records, bounding etcd/API growth during noisy self-service use. A repeat verifier exposed that admission journal names were idempotent only by claim name/generation, so recreated claims with the same name could be blocked by an old journal; the controller now includes a short claim UID hash plus a `claim-uid` label in journal identity. Rebuilt/imported `localhost/platform-provider-controller:dev` on n1/n2/n3 with manifest digest `sha256:72b992b69ec327e88b0fd3aa66f364e7d6fea456d157fe83cccadfb6e328efed` and runtime imageID `sha256:ce59564fd2b69db2ccb244329621ce28fb2b430bc9529d6db4ff0092ab89c236`. The portal summary cache TTL was raised from 5s to 30s and the read model now caps transient Kubernetes Events plus recent audit/journal records before serialization, so autoscaled conditional-read tests do not refresh every replica mid-burst or exceed the compact payload budget; rebuilt/imported `localhost/platform-provider-portal:dev` with manifest digest `sha256:3096fe58c5bcb0e2f006a25f9d1d83d715f6badcc70384159615509c34ba7109` and runtime imageID `sha256:7c7d848e3c999a70c13dc92b1e14cb21021829c406b4e807eb45c7008a47d9e7`. Passed gates: `verify-control-plane-retention.sh`, `verify-provider-api-contracts.sh`, `verify-provider-layer.sh`, `verify-flux-gitops.sh`, `verify-controller-sharding.sh`, `verify-provider-portal.sh`, `verify-api-fairness.sh`, and `verify-provider-api-load.sh` with 90 conditional reads across 3 Ready portal pods, `304_ratio=100.0%`, `bytes_saved=100.0%`, p95 `1332.5ms`, and warm summary size about `64KB`. |
| 2026-06-19 | Provider portal JWT write authentication | Passed | Replaced the portal's static-token primary write-auth path with signed JWT validation in the lab. `PORTAL_WRITE_AUTH_MODE=jwt` now validates issuer `https://issuer.platform.local`, audience `platform-portal`, HS256 signature from `platform-system/provider-portal-auth`, `exp`/`nbf`/`iat`, `sub`, `groups`, and `platform_namespaces`; tenant writes still require namespace scope plus the target `Project.spec.adminsGroup`, while `platform:admins` remains the provider-admin escape hatch. Static token mapping remains in the Secret only as rollback compatibility for `PORTAL_WRITE_AUTH_MODE=static`. Rebuilt/imported `localhost/platform-provider-portal:dev` on n1/n2/n3 with manifest digest `sha256:ff18e9c6256eaf5a0eee27e65d3d79624bd9b76b2b72a6db24d4fe74744f589a` and runtime imageID `sha256:b8e21c18bbb57eb1aa63ba7fbffa3574d46e646daf729d3f8f5257f141928f56`. Tightened provider-portal topology spread to `DoNotSchedule`, so the three-replica HA floor lands on n1/n2/n3 when all Hyper-V hosts are available; `verify-provider-portal.sh` now asserts the strict policy and three distinct Ready nodes. The verifier also mints short-lived lab JWTs and checks missing, invalid, expired, wrong-audience, wrong-tenant, valid tenant, shared rate-limit, audit, and cleanup paths. Follow-up gates passed: `verify-provider-api-load.sh` with 120 conditional reads across 4 Ready portal pods (`304_ratio=100.0%`, `bytes_saved=100.0%`, p95 `350.1ms`), `verify-provider-layer.sh`, `verify-api-fairness.sh`, and `verify-provider-api-contracts.sh`. |
| 2026-06-19 | CapacityCell failure-domain placement | Passed | Added `spec.placement.failureDomains` exact-match constraints for `VirtualMachineClaim` and `KubernetesClusterClaim` (`region`, `site`, `zone`, `rack`, `network`, `storage`). The provider controller filters candidate `CapacityCell` objects before readiness/scoring and rejects impossible constraints with `FailureDomainNotFound`; the portal VM/KCC write API and UI now pass optional zone/storage placement values through server-side allowlist validation. Rebuilt/imported `localhost/platform-provider-controller:dev` on n1/n2/n3 with manifest digest `sha256:cb58e51228899dd67849523a43ab0af1f8dae2a9424f5f1054b456f408d0926e` and runtime imageID `sha256:2fec15efaed954c41ac44fd7fbd33b856226aa4f7e05e79e6613fbc7e79d6e0b`; rebuilt/imported `localhost/platform-provider-portal:dev` with manifest digest `sha256:8324bd61059611c9e59d75b1e6cc53041f3b959a272b4f77e69cec316601f645` and runtime imageID `sha256:c16683a4a84c8bfd1b3a8b520acb432e6d905b3ec740ded988a86753dbe525ab`. Agent review caught that the first verifier used a synthetic Ready cell over real nodes; the verifier was tightened to use `lab-hyperv` for positive VM matching, verify impossible VM and KCC zones, and wait for cleanup of synthetic claims, reservations, and legacy cells. Passed gates: `verify-provider-api-contracts.sh`, `verify-provider-portal.sh`, `verify-provider-layer.sh`, `verify-controller-sharding.sh`, `verify-api-fairness.sh`, `verify-provider-api-load.sh` with 180 conditional reads across 6 portal pods (`304_ratio=100.0%`, `bytes_saved=100.0%`, p95 `339.8ms`), and `verify-flux-gitops.sh`. GitOps source was rebuilt after the IaC/documentation updates and Flux applied the updated platform revision. |
| 2026-06-19 | Flux strict spread and source failover | Passed | Tightened Flux HA verification and installation so `source-controller`, `kustomize-controller`, `helm-controller`, `notification-controller`, and `platform-gitops-source` use strict hostname topology spread across n1/n2/n3. Documented Flux `source-controller` as active/standby: exactly one Ready leader serves the artifact endpoint while two Running standbys participate in leader election. Changed the source-controller rollout strategy to `maxUnavailable=100%` so active/standby upgrades do not strand an old leader ReplicaSet. Added `FLUX_SOURCE_FAILOVER_SCOPE=leader-node-cordon` to `verify-flux-source-failover.sh`; the drill cordoned leader node n1, deleted leader pod `source-controller-65cbb974bc-nmpwb`, observed the leader move to `source-controller-65cbb974bc-bvbn5` on n3 with lease transitions increasing from 9 to 10, forced GitRepository/Kustomization reconcile without changing the applied platform revision, uncordoned n1, restarted source-controller, and restored three-node spread with a new leader on n1. `verify-flux-gitops.sh` passed afterward with all Flux controllers and `platform-gitops-source` distributed across n1/n2/n3 and provider objects still healthy. Follow-up gates passed: `verify-provider-layer.sh` and `verify-provider-api-load.sh` with 90 conditional summary reads across 3 Ready portal pods, `304_ratio=100.0%`, `bytes_saved=100.0%`, and p95 `580.5ms`. |
| 2026-06-19 | KubeVirt management HA and Hyper-V drill tooling | Passed | Added `apply-kubevirt-ha.sh` and `verify-kubevirt-ha.sh`, and extended the Flux-owned `kubevirt-configuration.yaml` so KubeVirt keeps `infra.replicas=3`, `workloads.replicas=3`, strict hostname topology spread, required pod anti-affinity, and PDB `minAvailable=2` for `virt-api` and `virt-controller`. Hardened `virt-operator` separately with three replicas, required hostname anti-affinity, strict spread, PDB `minAvailable=2`, and `maxUnavailable=1` to avoid rollout deadlock when all three Hyper-V-backed nodes are already occupied. A first rollout attempt exposed the interaction between required anti-affinity and `maxUnavailable=0`; the final manifest uses `maxUnavailable=1` for KubeVirt management components while PDBs preserve two healthy replicas. `verify-kubevirt-ha.sh` passed with `virt-api`, `virt-controller`, `virt-operator`, and `virt-handler` all healthy across n1/n2/n3. Added `hyperv-failure-drill.ps1` plus docs for elevated Hyper-V VM stop/start drills; destructive VM actions remain guarded by `-Execute -ConfirmDestructive`, disk operations fail closed, dry-run was verified, and non-destructive `WaitRecovery -Execute` confirmed SSH plus Kubernetes node readiness for `platform-n2`/`n2`. Follow-up gates passed: `verify-flux-gitops.sh`, `verify-provider-layer.sh`, `verify-provider-api-contracts.sh`, `probe-tenant-api-reachability.sh`, and `verify-provider-api-load.sh` with 90 conditional summary reads across 3 Ready portal pods, `304_ratio=100.0%`, `bytes_saved=100.0%`, and p95 `658.7ms`. The read-load verifier now warms each portal pod with bounded retry on transient `429`/`503` before starting the conditional-read phase, matching the portal's per-replica cache behavior after heavy write/API contract tests. |
| 2026-06-19 | Elevated Hyper-V restart and post-fault recovery | Passed | Ran an elevated `RestartVm` drill against `platform-n3`; `docs/logs/hyperv-failure-drills/20260619-145117-pid3048-hyperv-failure-drill.log` proves `dryRun=false`, `Stop VM 'platform-n3'`, and `Start VM 'platform-n3' after stop`. The first script version failed on a native `ssh.exe` timeout instead of retrying, so `hyperv-failure-drill.ps1` now treats native SSH failures as poll misses and keeps waiting. `docs/logs/hyperv-failure-drills/20260619-151016-pid20988-hyperv-failure-drill.log` then confirmed `SSH is ready`, Kubernetes node `n3` Ready, and `WaitRecovery` completed. Post-fault resource pressure made `virt-operator` liveness probes too aggressive for the 7 GiB nested nodes; `apply-kubevirt-ha.sh` now applies a startup probe plus slower liveness/readiness thresholds. Final checks passed: `verify-kubevirt-ha.sh`, `/readyz ok`, `n1/n2/n3 Ready`, no non-running pods, and Flux `GitRepository/platform-gitops` plus `Kustomization/platform-base` Ready at `main@sha1:02193d255b872114878fc6e02789ea38daf982ed`. |
| 2026-06-19 | Production cell substrate blueprint | Added | Added opt-in production-cell IaC under `iac/kubernetes/production-cell` plus `gitops/cells/production-cell-template`. The blueprint captures Rook-Ceph storage, Cilium LoadBalancer IPAM/BGP fabric, and Velero backup locations with explicit placeholders and `template-only` annotations so it is promotable to real cells without being applied to the current Hyper-V lab by default. Added `docs/production-cell-blueprint.md` and `iac/scripts/verify-production-cell-blueprint.ps1`. |
| 2026-06-19 | Production identity blueprint | Added | Added opt-in production OIDC/JWKS identity IaC under `iac/kubernetes/production-identity` plus `gitops/platform-production-identity`. The blueprint defines issuer discovery/JWKS placeholders, group and namespace claim mapping, provider-admin group mapping, and a portal Deployment env patch that removes lab static-token/HS256 env entries for a future JWKS runtime cutover. Added `docs/production-identity.md` and `iac/scripts/verify-production-identity-blueprint.ps1`; the current Hyper-V lab overlay remains on reproducible HS256 JWT auth until the portal code cutover is implemented. |
| 2026-06-19 | Production observability blueprint | Added | Added opt-in production observability IaC under `iac/kubernetes/production-observability` plus `gitops/platform-production-observability`. The blueprint defines HA Prometheus/Alertmanager contracts, provider-controller and provider-portal `ServiceMonitor` objects, provider SLO alert rules for controller leadership/reconcile freshness and portal error/throttle/cache health, plus logs/events/traces retention contracts for an OpenTelemetry Collector or Grafana Alloy style pipeline. Added `docs/production-observability.md` and `iac/scripts/verify-production-observability-blueprint.ps1`; the current Hyper-V lab overlay remains on lightweight direct verifiers until a real production observability stack is installed and validated. |
| 2026-06-19 | Production backup and DR blueprint | Added | Added opt-in production backup/DR IaC under `iac/kubernetes/production-backup` plus `gitops/platform-production-backup`. The blueprint layers the production-cell Velero locations with CSI `VolumeSnapshotClass` placeholders, Velero schedules for volume, namespace, and tenant-cluster targets, provider `BackupPlan` examples for remote/immutable destinations, and restore workflow contracts with collision, approval, and tenant-isolation policy. Added `docs/production-backup-dr.md` and `iac/scripts/verify-production-backup-blueprint.ps1`; the current Hyper-V lab remains on VM-target KubeVirt snapshot/copy-restore until the provider-controller runtime implements multi-target production backup/restore. |
| 2026-06-19 | Production backup runtime operations contract | Added | Extended the opt-in production backup/DR blueprint with runtime operations contracts for service ownership, Velero CSI data movement dependencies, BackupRepository health and maintenance, immutable object-store timing, RPO/RTO restore drills, and tenant-isolated restore policy. Added `docs/production-backup-operations.md` and `iac/scripts/verify-production-backup-runtime-blueprint.ps1`; no production backup runtime was applied to the Hyper-V lab overlay. |
| 2026-06-19 | Production backup runtime cutover skeleton | Added | Added opt-in `iac/kubernetes/production-backup-runtime` and `gitops/platform-production-backup-runtime` with a disabled-by-default `provider-backup-runtime-controller`, deployment-order gates, secret/object-store contract, canary-before-HA smoke test, rollback/fail-closed runbook, canary/HA patch examples, and `iac/scripts/verify-production-backup-runtime-cutover.ps1`; the Hyper-V lab overlay and contract-only production-backup bundle remain unchanged. |
| 2026-06-19 | Production backup runtime canary simulation | Added | Added local offline `iac/scripts/simulate-production-backup-runtime-canary.py` plus `iac/scripts/verify-production-backup-runtime-simulation.ps1`. The verifier compiles the simulator, parses deterministic `SIMULATION_JSON:`, checks Velero `Schedule`/`Backup`/`Restore` and status actions for `Volume`, `Namespace`, and `KubernetesClusterClaim`, validates preflight fields, proves fail-closed restore cases emit zero Velero actions, and confirms no live cluster/network client usage. This is an implementation contract only; the lab provider-controller remains VM backup/restore only. |
| 2026-06-19 | Production backup runtime API seam | Added | Extended the provider `RestoreRequest` CRD contract for `VirtualMachineClaim`, `Volume`, `Namespace`, and `KubernetesClusterClaim` targets, `Copy`/`InPlace` modes, Velero restore policy fields, source backup/schedule selection, and fail-closed validation status. Added a template-only runtime reconciliation contract documenting `BackupPlan` to Velero `Schedule`/`Backup` and `RestoreRequest` to Velero `Restore`, plus `iac/scripts/verify-production-backup-runtime-api-seam.ps1`. This is an API/runtime seam only; the Hyper-V lab provider-controller remains VM-copy restore only and rejects unsupported target kinds or modes with `UnsupportedTarget`. |
| 2026-06-19 | Production backup runtime live adapter | Added | Added local offline `iac/scripts/production-backup-runtime-live-adapter.py` plus `iac/scripts/verify-production-backup-runtime-live-adapter.ps1`. The verifier compiles the adapter, parses deterministic `ADAPTER_JSON:`, checks one Velero `Schedule`, `Backup`, `Restore`, and provider status set per `Volume`, `Namespace`, and `KubernetesClusterClaim` target, proves canary/preflight/tenant/policy/collision/in-place fail-closed cases create zero Velero manifests, checks no cluster/network/process access, and confirms the Hyper-V lab overlays do not reference the adapter. This is a disabled-by-default prototype only; the real production runtime integration remains open. |
| 2026-06-19 | Production scheduler and admission blueprint | Added | Added opt-in production scheduler/admission IaC under `iac/kubernetes/production-scheduler` plus `gitops/platform-production-scheduler`. The blueprint defines an HA `provider-scheduler` runtime contract, multi-cell filter/score/reserve/permit policy, transactional reservation/quota ledger contract, replayable `AdmissionJournal` usage, and a verifier `iac/scripts/verify-production-scheduler-blueprint.ps1`; the current Hyper-V lab overlay is intentionally unchanged until a real scheduler runtime is implemented and validated against multiple cells. |
| 2026-06-19 | Production scheduler runtime admission seam | Added | Added local offline `iac/scripts/production-scheduler-runtime-admission-seam.py` plus `iac/scripts/verify-production-scheduler-runtime-admission-seam.ps1`. The verifier compiles the seam, parses deterministic `ADMISSION_JSON:`, checks accepted VM and tenant Kubernetes decisions through `Filter`/`Score`/`Reserve`/`Permit`, verifies `ReservationIntent`, `QuotaAdmissionDecision`, `AdmissionJournal`, and status payloads, proves malformed, unknown-cell, quota, stale-capacity, lock, tenant-policy, unsupported-kind, and insufficient-capacity cases create zero reservation intents, checks no cluster/network/process access, and confirms the Hyper-V lab overlays do not reference the scheduler runtime seam. During live regression, `verify-provider-api-contracts.sh` was hardened to wait for asynchronous `Project.status.quotaUsage` publication instead of reading it once after restore cleanup. Remote gates passed from n1: provider API contracts, capacity admission lock, project quota admission lock, and claim lifecycle; remote temp scripts and all temporary claims/reservations/leases were cleaned. This is a disabled-by-default prototype only; the real production scheduler runtime remains open. |
| 2026-06-19 | Production scheduler runtime cutover skeleton | Added | Added opt-in `iac/kubernetes/production-scheduler-runtime` and `gitops/platform-production-scheduler-runtime` with a disabled-by-default `provider-scheduler-runtime-controller`, fail-closed `provider-scheduler-admission-webhook` contract, deployment-order gates, canary/HA patch examples, smoke-test runbook, rollback/fail-closed runbook, and `iac/scripts/verify-production-scheduler-runtime-cutover.ps1`. The verifier keeps the Hyper-V lab overlays and contract-only scheduler blueprint free of runtime cutover references. This is a promotion skeleton only; the real production scheduler runtime remains open. |
| 2026-06-21 | Production scheduler quota replay seam | Added | Added local offline `iac/scripts/production-scheduler-quota-replay-seam.py` plus `iac/scripts/verify-production-scheduler-quota-replay-seam.ps1`. The verifier compiles the seam, parses deterministic `QUOTA_REPLAY_JSON:`, checks VM and tenant Kubernetes `SchedulerQuotaReplay` happy paths with accepted `AdmissionJournal`, `CapacityReservation`, `Project` quota snapshots, and duplicate idempotency, proves malformed root, wrong canary scope, missing ledger inputs, stale reservations, conflicting duplicates, quota underflow/overflow repair, unknown request kind, and cross-project replay attempts commit zero repair actions, checks no cluster/network/process access, and confirms the Hyper-V lab overlays do not reference the quota replay seam. This is a disabled-by-default prototype only; the real production scheduler runtime remains open. |
| 2026-06-21 | Production scheduler replay operations contract | Added | Added `iac/kubernetes/production-scheduler-runtime/replay-operations-contract.yaml` and `iac/scripts/verify-production-scheduler-replay-operations.ps1` for disabled-by-default replay ownership, the `provider-scheduler-runtime-replay-owner` Lease, dry-run/canary/HA promotion gates, failover grace behavior, repair windows, replay audit events, tenant isolation, rollback boundary, and Hyper-V lab no-reference checks. This is an operations contract only; the real production scheduler runtime remains open. |
| 2026-06-21 | Production scheduler work-queue runtime contract | Added | Added `iac/kubernetes/production-scheduler-runtime/workqueue-runtime-contract.yaml` and `iac/scripts/verify-production-scheduler-workqueue-runtime.ps1` for disabled-by-default queue ownership, the `provider-scheduler-runtime-workqueue-owner` Lease, durable `SchedulerWorkQueueEvent` records, event ordering, idempotency, per-tenant weighted-round-robin fairness, duplicate suppression, backoff/dead-letter handling, failover handoff, workqueue metrics, audit events, and Hyper-V lab no-reference checks. This is a contract only; the real production scheduler runtime remains open. |
| 2026-06-21 | Production scheduler observability and SLO contract | Added | Added `iac/kubernetes/production-scheduler-runtime/observability-slo-contract.yaml` and `iac/scripts/verify-production-scheduler-observability-slo.ps1` for disabled-by-default health/readiness endpoints, queue/replay/admission metric families, SLO objectives, burn-rate alert windows, failure-domain drill evidence, tenant fairness dashboard fields, audit retention freshness signals, owner escalation, and Hyper-V lab no-reference checks. This is a contract only; the real production scheduler runtime remains open. |
| 2026-06-21 | Production backup service runtime integration contract | Added | Added `iac/kubernetes/production-backup-runtime/service-runtime-integration-contract.yaml` and `iac/scripts/verify-production-backup-service-runtime-integration.ps1` for disabled-by-default controller ownership, object-store secret and Velero location wiring, `Volume`/`Namespace`/`KubernetesClusterClaim` target mapping, immutable storage expectations, ordered restore workflow phases, preflight gates, status/audit events, SLO alert hooks, tenant-isolation boundaries, fail-closed negative probes, and Hyper-V lab no-reference checks. This is a contract only; the real production backup controller remains open. |
| 2026-06-21 | Production backup live controller contract | Added | Added `iac/kubernetes/production-backup-runtime/live-controller-contract.yaml`, local offline `iac/scripts/production-backup-live-controller.py`, and `iac/scripts/verify-production-backup-live-controller.ps1` for disabled-by-default runtime image handoff, reconcile states, Velero creation boundaries, idempotency keys, status/audit/SLO publication, fail-closed rejected paths, no live cluster access, and Hyper-V lab no-reference checks. This is an implementation contract only; the Hyper-V lab runtime remains disabled. |
| 2026-06-21 | Production backup controller runtime image seam | Added | Added `iac/kubernetes/production-backup-runtime/runtime-image-seam.yaml`, local offline `iac/scripts/production-backup-controller-runtime-image-seam.py`, and `iac/scripts/verify-production-backup-controller-runtime-image-seam.ps1` for the disabled-by-default executable controller image seam. The verifier checks image/entrypoint/env handoff, `BackupPlan` and `RestoreRequest` fixtures for `Volume`, `Namespace`, and `KubernetesClusterClaim`, deterministic Velero/status/audit/SLO/idempotency/promotion payloads, fail-closed negative probes including enabled-without-canary and cross-tenant restore, no live cluster access, and Hyper-V lab no-reference checks. |
| 2026-06-21 | Production platform runtime blockers closure | Added | Added `docs/platform-runtime-blockers-closure.md` and `iac/scripts/verify-production-platform-runtime-blockers-closure.ps1` to tie production scheduler runtime replacement and scheduler-grade quota replay to remaining CAPK larger-cell and OIDC/JWKS runtime gaps. The verifier reruns scheduler runtime, quota replay, replay operations, workqueue, observability, cutover, and blueprint checks, then confirms Hyper-V lab overlays remain untouched. |
| 2026-06-21 | CAPK/OIDC runtime cutover readiness | Added | Added `docs/capk-oidc-runtime-cutover-readiness.md`, `iac/kubernetes/production-cell/capk-oidc-runtime-cutover-readiness.yaml`, and `iac/scripts/verify-production-capk-oidc-runtime-cutover-readiness.ps1` for disabled-by-default larger-cell CAPK and external OIDC/JWKS runtime evidence gates. The verifier validates contracts, live evidence schema, fail-closed behavior, no live mutation, and Hyper-V lab no-reference checks; final aggregate completion still requires an actual larger-cell/OIDC live evidence file. |
| 2026-06-21 | Live larger-cell CAPK/OIDC preflight | Added | Added `docs/live-larger-cell-capk-oidc-evidence.md`, `iac/scripts/invoke-live-larger-cell-capk-oidc-preflight.ps1`, and `iac/scripts/verify-live-larger-cell-capk-oidc-preflight.ps1`. The preflight uses read-only Hyper-V inventory, SSH `sudo -n k3s kubectl`, and bounded WSL/bash probes to produce either live-ready evidence or a precise blocker report without claiming aggregate completion. Current G018 preflight blocks on host free memory, tenant cluster shape, tenant control-plane VMI count, and provider portal OIDC/JWKS runtime cutover. |
| 2026-06-21 | Actual CAPK/OIDC live cutover gate | Added | Added `iac/scripts/verify-actual-capk-oidc-live-cutover.ps1` plus `.omo/ulw-loop/evidence/actual-capk-oidc-live-cutover-report.json` and progress evidence for the current G023 pass. The gate confirms the current host still lacks safe non-disruptive memory for larger-cell promotion, tenant-a remains CP `1/1/1` with workers `0/0/0`, provider portal remains `jwt`/HS256 without JWKS, no live mutations were performed, and aggregate completion remains blocked until a live EvidenceFile is accepted. |
| 2026-06-21 | Live verification CLI and Flux readiness alignment | Passed | Installed portable Windows live-verification CLIs in `C:\Users\yuri\bin`: `kubectl` v1.35.5 matched to k3s v1.35.5, `helm` v3.21.2, `flux` 2.8.8, and `virtctl` v1.8.4 matched to the live KubeVirt server. Updated Flux GitOps IaC default to v2.8.8 and patched source-controller HA readiness to `/readyz` on `healthz`, matching the upstream Flux v2.8.x HA workaround so all three source-controller replicas are Ready while Lease leader election still controls the active reconciler. Raised source-controller PDB to `minAvailable=2`, added `minDomains=3` plus `matchLabelKeys=[pod-template-hash]` to strict hostname topology spread for Flux/GitOps and provider portal HA pods so recovered nodes and rolling updates do not leave the platform over-concentrated, updated GitOps/failover verifiers and docs from the old single-Ready-leader assumption to Lease-holder semantics, and prepared the live cluster for standard `flux check` plus existing HA-aware reconciliation gates. |
| 2026-06-21 | Provider portal OIDC/JWKS live runtime | Passed | Rebuilt and imported `localhost/platform-provider-portal:dev` on n1/n2/n3 with manifest digest `sha256:173559f1fbd4fca22e836941a9e14c68812dc875d8d26447cb6ebe7904986c98`, rebuilt/imported `localhost/platform-gitops-source:dev` with digest `sha256:7e71bf847529ffdca234674b545005ba6d72f687b656810dd97d300c8af846da`, and reconciled Flux to `main@sha1:47afff9e089e6b84ce97fb1ab0f2304911eb201b`. Provider portal runtime now uses `PORTAL_WRITE_AUTH_MODE=oidc-jwks`, `PORTAL_JWT_ALLOWED_ALGORITHMS=RS256`, and the lab JWKS URI without HS256 env fallback. `verify-provider-portal.sh` passed across five autoscaled portal pods, rejecting unauthenticated, HS256 downgrade, tampered, expired, wrong-audience, cross-tenant, and noisy write calls while creating/deleting temporary Project, VM, tenant Kubernetes, volume, network, firewall, and access resources with audit and cleanup. `flux check --timeout=120s` and `verify-flux-gitops.sh` passed with GitRepository and Kustomization Ready at the same revision. |
| 2026-06-21 | Neutral provider API namespace migration guard | Added | Replaced repository and documentation vendor-specific API labels/groups with `platform.privatecloud.local`, removed generated `.tmp` snapshots, and added `iac/scripts/migrate-provider-api-group.sh` plus `docs/provider-api-group-migration.md`. The migration helper discovers any previous provider API group dynamically from installed CRDs, applies target CRDs, copies provider API resources, and keeps CRD retirement behind a separate explicit confirmation gate. Local checks passed: provider Python compile, shell syntax for all scripts, `kubectl kustomize --load-restrictor=LoadRestrictionsNone gitops/platform`, production identity blueprint, CAPK/OIDC runtime readiness, aggregate runtime blockers closure, and workspace forbidden-name scan. |
| 2026-06-22 | Live verification tooling | Passed | Installed portable local Windows verification CLIs in `C:\Users\yuri\bin` and management-node Linux CLIs on n1/n2/n3: `helm` v3.21.2, `flux` 2.8.8, `clusterctl` v1.13.2, `kubeconform` v0.8.0, `conftest` v0.68.2, `yq` v4.53.3, and `jq` 1.8.2. Local Windows also has `kubectl` v1.35.5 and `virtctl` v1.8.4. Added `iac/scripts/install-live-verification-tooling.ps1` for repeatable installs. |
| 2026-06-22 | Tenant cluster live recovery and verification | Passed | Recreated `tenant-a/routable-cluster` in the neutral provider API group, recovered kubeadm bootstrap after the nested-virtualization control-plane health check timeout, installed Cilium with a direct bootstrap API endpoint, and verified CAPI `Cluster`, `KubeadmControlPlane`, `Machine`, `KubeVirtMachine`, tenant node, CoreDNS, and Cilium all Ready/Running. `verify-tenant-cluster-layer.sh` passed through routed endpoint `172.28.10.102:6443`. |
| 2026-06-22 | kubeadm/CNI bootstrap hardening | Added | Provider controller and tenant CAPI template now render kubeadm init/join timeout fields suitable for slow nested Hyper-V bootstrap, and `install-tenant-cni.sh` accepts `CILIUM_K8S_SERVICE_HOST`/`CILIUM_K8S_SERVICE_PORT` for Cilium bootstrap before Kubernetes service routing is available. Python compile and YAML parse checks passed. |
| 2026-06-22 | Boxed-product operations coverage | Added | Crawled the public external operations WebHelp TOC/search index, identified 4,320 pages and a 4,147-page operations-oriented subset, and added `docs/boxed-product-operations-coverage.md` mapping provider, reseller, customer, catalog, subscription, provisioning, security, support, monitoring, backup/DR, reporting, API, and upgrade scenarios to implemented evidence and promotion gates. |
| 2026-06-22 | Provider API legacy CRD retirement | Passed | `repair-provider-ownerreferences.sh` reported zero child owner reference repairs from n1 and n3. Retired the previous provider API CRDs behind `MODE=retire` and both explicit confirmation flags, removed finalizers from the three deleting legacy objects, and confirmed discovery now exposes only `platform.privatecloud.local`; short names resolve unambiguously and provider, tenant, portal, and routed endpoint verifiers passed. |
| 2026-06-22 | Post-cleanup live health, tooling, and verifier check | Passed | Confirmed `n1`, `n2`, and `n3` are `Ready`, no pods are outside `Running`/`Succeeded`, `tenant-a/routable-cluster` is `Ready` on `172.28.10.102:6443`, and provider CRD discovery exposes only `platform.privatecloud.local`. Rechecked local Windows CLI versions for `kubectl`, `helm`, `flux`, `virtctl`, `clusterctl`, `kubeconform`, `conftest`, `yq`, and `jq`; rechecked remote n1/n2/n3 Linux CLI versions for `helm`, `flux`, `clusterctl`, `kubeconform`, `conftest`, `yq`, and `jq`. Re-ran `verify-provider-layer.sh`, `verify-tenant-cluster-layer.sh`, `verify-provider-portal.sh`, and `probe-tenant-api-reachability.sh` from n1; provider-controller shards ended `3/3` plus `3/3`, provider portal scaled to `9/9` after smoke load, routed tenant API probes returned `200 OK` from every provider-controller pod, and all smoke resources cleaned up. |
| 2026-06-22 | Product catalog and subscription lifecycle | Passed | Added `ProductPlan` and `Subscription` provider API contracts, RBAC, examples, controller status reconciliation, portal summary/write paths, and verifier coverage. The live API now publishes a demo VM plan, activates a tenant subscription when the referenced Project and plan are valid, rejects invalid catalog/subscription cases through the contract verifier, and lets the portal smoke create/delete a temporary admin-only ProductPlan plus tenant Subscription with durable audit records. Full order queue, renewal, hold/cancel, upgrade/downgrade, rate cards, and billing workflows remain production promotion work rather than lab completion claims. |
| 2026-06-22 | Backup recovery point and GitOps drift hardening | Passed | Hardened BackupPlan reconciliation to recognize existing KubeVirt snapshots from owner references, normalize provider labels, and avoid duplicate recovery points after API-group cleanup. Refreshed live RBAC/GitOps state so provider-controller shards can list and update product plans and subscriptions after Flux reconciliation. `tenant-a/demo-daily` reports `Protected` with a ready recovery point, `verify-provider-api-contracts.sh` passed, and `verify-flux-gitops.sh` stayed green after reconciliation. |
| 2026-06-22 | Windows-origin live gate suite | Passed | Re-ran the live gates from this workstation against `https://172.28.10.11:6443` using the portable `kubectl.exe` path: `verify-provider-portal.sh`, `verify-provider-api-contracts.sh`, `verify-flux-gitops.sh`, `verify-provider-layer.sh`, `verify-tenant-cluster-layer.sh`, and `probe-tenant-api-reachability.sh` all exited `0`. The portal smoke now tampers a significant JWT signature byte, so the invalid-signature case is a real `401` check instead of a base64 padding artifact. |
| 2026-06-22 | Durable order provisioning lifecycle | Passed | Added the tenant-scoped `Order` provider API contract, RBAC, desired example, provider-controller reconciliation, portal UI/write API support, audit enum coverage, and live verifier coverage. The lab now proves an idempotent `CreateSubscription` order with policy/approval fields, status, and target subscription tracking; `tenant-a/tenant-a-vm-basic-order` is `Succeeded` and created `tenant-a/tenant-a-vm-basic-ordered` as an `Active` subscription, while the contract verifier rejects a missing-plan order with `ProductPlanNotFound` and no subscription. Rebuilt/imported `localhost/platform-provider-controller:dev` on n1/n2/n3 with manifest digest `sha256:5217ad82b7e2b7d032d39d46d213506ec6c830a5936d4b9eb82643fe0ee78378` and runtime imageID `sha256:6651843231777a841a5e71c7d41e7e0b76ad3872757af0cd711b5c507178e515`; rebuilt/imported `localhost/platform-provider-portal:dev` with manifest digest `sha256:1ecf1d138658296c14a79db365dbfb95f541f73e3b7a078b5e3923a2c66a27cb` and runtime imageID `sha256:c82510fa198f97f99386aaa533977810cb147f81f08f84ede69b25ae09da8a63`. Rebuilt/imported `localhost/platform-gitops-source:dev` on n1/n2/n3 and reconciled Flux to the updated in-cluster Git source. Passed gates: local Python compile, shell syntax, `kubectl kustomize`, `verify-provider-api-contracts.sh`, `verify-provider-portal.sh`, `verify-flux-gitops.sh`, `verify-provider-layer.sh`, `verify-controller-sharding.sh`, `verify-tenant-cluster-layer.sh`, `probe-tenant-api-reachability.sh`, `verify-api-fairness.sh`, and `verify-provider-api-load.sh`. Full renewal, cancel/hold, upgrade/downgrade, rate-card, and production queue workflows remain promotion work, not lab completion claims. |
| 2026-06-22 | Durable order subscription changes | Passed | Extended provider-controller reconciliation for durable `Order` actions `ChangeSubscription`, `RenewSubscription`, and `CancelSubscription` alongside `CreateSubscription`. The controller now validates Project/Subscription ownership, published target plans, active/cancelled state boundaries, policy and approval gates, idempotent subscription spec patching, and terminal order status once subscription status converges. Rebuilt/imported `localhost/platform-provider-controller:dev` on n1/n2/n3 with manifest digest `sha256:ffac05afbe96269f037bb14bf019084dd8d0a9bdbcdb2e532a9a7624b2c74131` and runtime imageID `sha256:267be2f91d881790adff2519a257ebc6ac2c976c38f8198569f8ccf1bb4181df`; rebuilt/imported `localhost/platform-provider-portal:dev` with manifest digest `sha256:e448c163643231eb4b00cae397a1dc85e52a1b0307e00ae19200e778ed7e32ae` and runtime imageID `sha256:24bc43b2e9cbe5bb6c8e301542fe4f15f0f5e2d625d7fb28ad749db37b78416e`. The contract verifier now creates a temporary lifecycle plan/subscription, changes the subscription plan, renews `autoRenew`, cancels the subscription, and confirms rejected missing-plan orders still create no subscription. Portal smoke now creates a subscription order, then renewal and cancellation orders through HTTP write APIs, verifies audit create/delete records, and cleans all temporary objects. Passed gates from this workstation: Python compile, shell syntax, `kubectl kustomize`, `verify-provider-api-contracts.sh`, `verify-provider-portal.sh`, `verify-flux-gitops.sh`, `verify-provider-layer.sh`, `verify-controller-sharding.sh`, `verify-tenant-cluster-layer.sh`, `probe-tenant-api-reachability.sh`, `verify-api-fairness.sh`, and `verify-provider-api-load.sh`. Remaining production promotion work is durable queue orchestration, hold/suspension, delayed change windows, dependency-aware cancellation, retry/dead-letter controls, rate-card/billing integration, and full commercial operations. |
| 2026-06-22 | Flux source artifact endpoint hardening | Passed | Live forced reconcile exposed that making all `source-controller` pods Ready via `/readyz` on `healthz` allowed the `source-controller` Service to route kustomize-controller artifact downloads to standby pods that do not serve port `9090`, producing intermittent `connection refused`. Reverted the lab HA contract to artifact-serving readiness (`/` on `http`), `maxUnavailable=2`, PDB `minAvailable=1`, and verifier expectations of three Running pods with exactly one Ready Service endpoint matching the Lease holder. Applied the live patch, performed a clean source-controller handoff, rebuilt/imported the GitOps source image on n1/n2/n3, reconciled Flux successfully to the updated source, and reran gates: `verify-flux-gitops.sh`, `verify-provider-layer.sh`, `verify-controller-sharding.sh`, `verify-tenant-cluster-layer.sh`, `verify-provider-api-contracts.sh`, `verify-provider-portal.sh`, `probe-tenant-api-reachability.sh`, `verify-api-fairness.sh`, and `verify-provider-api-load.sh`. |
| 2026-06-22 | Durable subscription hold and resume orders | Passed | Extended the `Order` provider API, provider-controller reconciliation, portal write validation, CRD schema, contract verifier, and portal smoke to support `SuspendSubscription` and `ResumeSubscription` actions. The live controller now validates target subscription ownership and plan consistency, rejects cancelled subscriptions, patches `Subscription.spec.state` to `Suspended` or `Active`, and completes the order only after `Subscription.status.phase` converges. Rebuilt/imported `localhost/platform-provider-controller:dev` on n1/n2/n3 with manifest digest `sha256:c14578ed54a46fb7d745727a1888d137c33eeeb26b2cf43776df7f699a1f8e9d` and runtime imageID `sha256:8842e2de63b6729b0ad3ff5e743f5fa46400d02ef07530e29d7e18955c65e245`; rebuilt/imported `localhost/platform-provider-portal:dev` with manifest digest `sha256:f788d4b9957286525696f383314c3bb4ae97da1565c2e91f6e058114f311ef0c` and runtime imageID `sha256:4c14855614eb3f341861813b8298dcab63be57be506bd02eb6b82e47543c7739`. Contract verification now proves plan change, suspend, resume, renew, and cancel against a temporary subscription; portal smoke proves the same suspend/resume actions through HTTP write APIs with audit create/delete records and cleanup. Passed gates: Python compile, shell syntax, `kubectl kustomize`, `verify-provider-api-contracts.sh`, `verify-provider-portal.sh`, `verify-flux-gitops.sh`, `verify-provider-layer.sh`, `verify-controller-sharding.sh`, `verify-tenant-cluster-layer.sh`, `probe-tenant-api-reachability.sh`, `verify-api-fairness.sh`, and `verify-provider-api-load.sh`. Remaining production promotion work is durable queue orchestration, dependency-aware hold/cancel consequences, delayed change windows, retry/dead-letter controls, rate-card/billing integration, and richer lifecycle UX. |
| 2026-06-22 | Provider portal fresh-summary and live load hardening | Passed | Raised the live portal HA floor to `6` replicas with HPA `6-9` and PDB `minAvailable=4`, kept topology spread across n1/n2/n3, and rebuilt/imported `localhost/platform-provider-portal:dev` on all nodes with manifest digest `sha256:d03d80f7e82c23af0ef061b9a508c4e879dbdc5639ef769e283cbe9f25274812` and runtime imageID `sha256:cdfba8e21b5971b733304ffd4dfeb43d6ff79d31426175dec9310cb875d71e9f`. `/api/summary?fresh=1` now synchronously refreshes on a replica when its refresh lock is free and otherwise uses bounded stale/retry behavior; portal summary retention is capped for audit/event-heavy payloads, and the verifier uses unique smoke IDs, hard kubectl process timeouts, endpoint rotation, JWK env handoff, and retrying fresh-summary/audit checks. `probe-tenant-api-reachability.sh` now labels direct tenant ClusterIP/VMI probes as optional diagnostics while keeping routed tenant API endpoints as the hard gate, and `verify-provider-api-load.sh` waits for `fresh=1`/`health.ready=true` on every Ready portal pod before measuring conditional reads. Passed live gates: `verify-provider-portal.sh`, `verify-flux-gitops.sh`, `verify-provider-api-contracts.sh`, `verify-provider-layer.sh`, `verify-controller-sharding.sh`, `verify-tenant-cluster-layer.sh`, `probe-tenant-api-reachability.sh`, `verify-api-fairness.sh`, and `verify-provider-api-load.sh` (`180` conditional reads across `6` pods, `304_ratio=100%`, `bytes_saved=100%`, `p95=528.2ms`). |
| 2026-06-22 | Provider portal MVP UX polish | Passed | Updated the live `Platform Console` UI to use working resource navigation filters, tenant search, tenant/admin action tabs, empty states, audit/admission rails, safer destructive-action exposure, a compact mobile navigation layout, `/favicon.ico` no-content handling, and an initial fresh summary request for the first user-facing health badge. Larger-cell, public-network, and dedicated-placement choices remain hidden from the default MVP UI while they stay promotion-gated. Rebuilt/imported `localhost/platform-provider-portal:dev` on n1/n2/n3 with manifest digest `sha256:31513059e90e63957629382322b86acbf3905dcdd94b942cacdf986a304163da` and final runtime imageID `sha256:cdd16cdae68ef177a64a5823ba48d2a58a7aea5372d3e9a62676c6717bc621b0`; all six Ready portal pods ran that runtime image, spread two per node. Fresh summary returned `health.ready=true` and `nonRunningPods=0`. Browser QA against `http://172.28.10.101/` with Chrome desktop `1440x950` and mobile `390x844` passed page identity, nonblank render, no framework overlay, clean console, screenshot evidence, navigation/filter empty-state, tenant/admin action-tab, and hidden-destructive-without-token checks. Screenshots were saved outside the repo at `C:/Users/yuri/AppData/Local/Temp/platform-portal-qa-desktop.png` and `C:/Users/yuri/AppData/Local/Temp/platform-portal-qa-mobile.png`. |
