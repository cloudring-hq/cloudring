# Directory Binary Text-Like Context

This run refines text-like binary backlog by decoding for classification and controlled counters only. It writes no raw decoded text, paths, file names, domains, URLs, people, provider names, brand names, vendor names, or secrets. Credential-like text remains marker-only and excluded from decoded content handling.

## Agent Use

- Use `binary-textlike-cases.jsonl` for configuration, project-state, export, backup, and operational-residue situations.
- Use `binary-textlike-signals.jsonl` only for stable ids, decode statuses, redaction/count buckets, and controlled tags.
- Treat sensitive and credential-like statuses as backlog for secret-aware handling, not training payload.
- Never reconstruct raw text, paths, source names, domains, brands, or secrets from this layer.

## Largest Binary Text-Like Cases

- `binarytextlikecase-0a53b8068f61` (62 signals): A text-like binary cluster refined text like binary decoded metadata evidence for networking connectivity; the dominant signal is contractual process.
- `binarytextlikecase-751ad0adf90c` (21 signals): A text-like binary cluster refined billing contract evidence for compute virtualization; the dominant signal is billing friction.
- `binarytextlikecase-bf4dc4499799` (16 signals): A text-like binary cluster refined text like binary decoded metadata evidence for networking connectivity; the dominant signal is context signal.
- `binarytextlikecase-820095105147` (9 signals): A text-like binary cluster refined credential text excluded evidence for general business context; the dominant signal is context signal.
- `binarytextlikecase-d104e1b75713` (7 signals): A text-like binary cluster refined compute storage evidence for networking connectivity; the dominant signal is contractual process.
- `binarytextlikecase-743b73e8273c` (6 signals): A text-like binary cluster refined text like binary decoded metadata evidence for networking connectivity; the dominant signal is contractual process.
- `binarytextlikecase-c8ac1867d69b` (6 signals): A text-like binary cluster refined compute storage evidence for networking connectivity; the dominant signal is contractual process.
- `binarytextlikecase-32106a0f3915` (5 signals): A text-like binary cluster refined text like binary decoded metadata evidence for storage backup; the dominant signal is data loss recovery.
- `binarytextlikecase-605ee7880e25` (5 signals): A text-like binary cluster refined text like binary decoded metadata evidence for networking connectivity; the dominant signal is contractual process.
- `binarytextlikecase-9bf461191c45` (3 signals): A text-like binary cluster refined compute storage evidence for networking connectivity; the dominant signal is contractual process.
- `binarytextlikecase-df534ee641af` (2 signals): A text-like binary cluster refined billing contract evidence for networking connectivity; the dominant signal is contractual process.
- `binarytextlikecase-84990859cf01` (2 signals): A text-like binary cluster refined compute storage evidence for networking connectivity; the dominant signal is contractual process.
- `binarytextlikecase-1c1b3ca87a3f` (2 signals): A text-like binary cluster refined compute storage evidence for networking connectivity; the dominant signal is contractual process.
- `binarytextlikecase-427d5dfc14cf` (1 signals): A text-like binary cluster refined credential text excluded evidence for networking connectivity; the dominant signal is context signal.
- `binarytextlikecase-28490af7bdaf` (1 signals): A text-like binary cluster refined access secret management evidence for general business context; the dominant signal is context signal.
- `binarytextlikecase-a9ab49f63dfe` (1 signals): A text-like binary cluster refined general text signal evidence for networking connectivity; the dominant signal is contractual process.
- `binarytextlikecase-5d0ae7c55c28` (1 signals): A text-like binary cluster refined billing contract evidence for compute virtualization; the dominant signal is contractual process.
- `binarytextlikecase-70aaabdff203` (1 signals): A text-like binary cluster refined general text signal evidence for networking connectivity; the dominant signal is contractual process.
- `binarytextlikecase-af0bfb91baa0` (1 signals): A text-like binary cluster refined access secret management evidence for networking connectivity; the dominant signal is contractual process.
